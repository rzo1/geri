# Copy to .env and fill in. CLI flags override these values.
GITLAB_URL=https://gitlab.example.com
# scope: api (deleting needs write access; Owner role on the group/project)
GITLAB_TOKEN=glpat-xxxxxxxxxxxxxxxxxxxx
